class Customer {
    constructor(id, fullname, email, birthdate, createdAt, updatedAt) {
        this.id = id;
        this.fullname = fullname;
        this.email = email;
        this.birthdate = birthdate;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}

export default Customer;